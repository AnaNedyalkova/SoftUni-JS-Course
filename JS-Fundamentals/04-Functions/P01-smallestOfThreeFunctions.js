let printMinNum = (n1,n2,n3) => Math.min(n1,n2,n3);
console.log(printMinNum(1,3,4));
 //same with arrow function

function printMinNumber(n1, n2, n3) {

    let minNum = Math.min(n1, n2, n3);
    console.log(minNum)
}
printMinNumber(2, 5, 3)